-- Insert test users (run after creating accounts via auth)
-- You'll need to sign up these users first, then get their UUIDs and run:

-- Example (replace with actual UUIDs after signup):
-- INSERT INTO users (id, name, email, role, location) VALUES 
-- ('uuid-from-auth', 'Test User', 'user@example.com', 'user', 'Downtown'),
-- ('uuid-from-auth', 'Test Worker', 'worker@example.com', 'worker', 'Uptown'),
-- ('uuid-from-auth', 'Test Admin', 'admin@example.com', 'admin', 'City Center');